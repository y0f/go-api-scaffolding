version: "2"
sql:
  - engine: postgresql
    schema: migrations
    queries:
      # forge:begin example
      - internal/modules/widget/queries.sql
      # forge:end example
      # forge:begin outbox
      - internal/outbox/queries.sql
      # forge:end outbox
      # forge:begin idempotency
      - internal/idempotency/queries.sql
      # forge:end idempotency
    gen:
      go:
        package: db
        out: internal/gen/db
        sql_package: pgx/v5
        emit_interface: true
        emit_pointers_for_null_types: true
        emit_empty_slices: true
        emit_exact_table_names: false
        query_parameter_limit: 4
        overrides:
          - db_type: uuid
            go_type: github.com/google/uuid.UUID
          - db_type: timestamptz
            go_type: time.Time
