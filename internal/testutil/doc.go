// Package testutil provides integration-test helpers. The Postgres helpers are
// behind the "integration" build tag so the default unit-test loop stays fast
// and does not require Docker.
package testutil
