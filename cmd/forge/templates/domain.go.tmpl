// Code generated by forge. Edit freely; it is yours now.
package {{.Snake}}

import (
	"errors"
	"strings"
)

// Input is the data needed to create a {{.Snake}}. Forged routes are not in
// api/openapi.yaml, so they get no schema validation from the request
// validator: Validate is the only check.
type Input struct {
	Name string
}

func (in Input) Validate() error {
	if strings.TrimSpace(in.Name) == "" {
		return ErrInvalidInput
	}
	return nil
}

var (
	ErrNotFound     = errors.New("{{.Snake}} not found")
	ErrForbidden    = errors.New("forbidden")
	ErrInvalidInput = errors.New("name is required")
)
