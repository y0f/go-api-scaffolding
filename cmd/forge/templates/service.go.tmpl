// Code generated by forge. Edit freely; it is yours now.
package {{.Snake}}

import (
	"context"
	"log/slog"

	"github.com/google/uuid"

	dbgen "{{.Module}}/internal/gen/db"
)

// Service holds the business rules for {{.Snake}}. Add authorization and
// validation here.
type Service struct {
	repo   Repository
	logger *slog.Logger
}

func NewService(repo Repository, logger *slog.Logger) *Service {
	return &Service{repo: repo, logger: logger}
}

func (s *Service) Create(ctx context.Context, in Input) (dbgen.{{.Pascal}}, error) {
	return s.repo.Create(ctx, in)
}

func (s *Service) Get(ctx context.Context, id uuid.UUID) (dbgen.{{.Pascal}}, error) {
	return s.repo.Get(ctx, id)
}

func (s *Service) List(ctx context.Context, limit, offset int32) ([]dbgen.{{.Pascal}}, error) {
	return s.repo.List(ctx, limit, offset)
}

func (s *Service) Delete(ctx context.Context, id uuid.UUID) error {
	return s.repo.Delete(ctx, id)
}
